import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sample-state-management',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
